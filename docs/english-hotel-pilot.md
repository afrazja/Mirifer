# English lesson 1: A quieter room

Entry: choose English at `/languages`, then `/practice/english`. English has a single experimental lesson, not a full beginner curriculum. German and the public landing pages remain unchanged.

## Conversation

The authored engine in `src/lib/practice/hotel.ts` runs entirely in the browser. The exchange advances one learner reply at a time: explain the noise, identify room 204, hear two room options, choose or ask about them, check the cost of quiet room 512, confirm the move, and recall the price question in a second situation. A replay switches the noisy option between a room beside the lift and one facing the street. The receptionist acknowledges each decision and asks the next question; optional clarification and directions follow prepared branches. The live scene shows only the current turn's guidance, not a list of questions to answer up front.

For each eligible reply, the existing Gemini → DeepSeek → OpenAI chain checks whether its meaning fits Jamie's current question. Natural paraphrases and imperfect English can select an authored intent; a related reply without a decision becomes an acknowledged turn at the same stage. The model may suggest a corrected sentence and a short English/Persian tip. It cannot invent a receptionist line or skip a goal. Empty, oversized and clear mission-conflict replies stay with the authored coach. The explicit phrase matcher remains a fallback when AI cannot respond, so prepared examples still work. Completion records a successfully navigated authored path, not language proficiency or pronunciation accuracy.

Reception lines play automatically when a learner starts the scene or advances a turn while voice is on; each line also has a replay button. Browser speech synthesis prefers a local English voice and falls back to another browser English voice or its default English setting. The learner can turn automatic voice off. The visible Record answer button uses browser English speech recognition to fill the answer box; the learner reviews the transcript and explicitly sends it. Browsers without speech recognition keep typed answers and examples available. A separate optional practice recorder captures at most 45 seconds for local playback only. Its tracks stop on stop, closing the panel, timeout, turn change and component disposal. A pending permission request cannot start recording after the panel closes. Mirifer never uploads, scores or persists raw audio. The browser's recognition or voice service may process audio or authored receptionist text, as disclosed in the privacy policy.

## Data and cost boundaries

- Refresh/resume: accepted replies, AI-resolved choice IDs and used hint stages in account-scoped `sessionStorage`, keyed by course and scenario. The draft is validated by replaying the engine; corrupt/out-of-order drafts are discarded. It is removed when completion saves successfully. It lasts only in the current tab session.
- Completion: verified Supabase user, active English course, validated branch-ID trail, timestamp, scenario variant and number of hint stages. Only the bounded `user_metadata.english_hotel_v1` value is written. No learner-written text is submitted with completion, no SQL migration is needed, and German progress/XP are never read or written by the pilot.
- Tracker: existing conversation/answer/hint events, with allowlisted `course: en`, `scenario: hotel-quiet-room-v1`, `page: practice` and `ai_rescued` on submitted answers. No lesson day/attempt is attached. Here `correct` means a supported reply was understood; it is not a grammar grade. Raw text and recordings are excluded. The existing lesson funnel is still for German; no new course-level admin reporting is claimed.
- AI: each eligible reply (300 characters maximum), the current step and scenario branch go to the existing provider chain. The authenticated server limits attempts to 18 per learner per UTC day using text-free events; the UI distinguishes that limit from a provider outage. AI responses are schema-checked and constrained to current authored intents or a same-stage acknowledgement. No new SQL or environment variables are needed. Provider requests may incur cost.

## Validation

Test both scene variants, normal paraphrases, AI-rescued paraphrases, common corrections, negated/out-of-context input, wrong room number, premature acceptance, AI unavailability, recall, tampered completion trails, account checks, save failures and per-account draft isolation. Browser-check a new English selection, a complete run, refresh/resume, failed-save retry, replay, course switching, mobile, Persian/RTL, dark mode and keyboard operation. Verify audio is not uploaded.
